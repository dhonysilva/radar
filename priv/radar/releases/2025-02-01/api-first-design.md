---
title: "API-First Design"
ring: adopt
quadrant: techniques
tags: [api, architecture]
related: [contract-testing, spec-driven-development]
---

**API-first design** means writing the API contract before any implementation code, so consumers and providers can build in parallel against a shared spec. It's paired naturally with contract testing to keep that promise honest.
